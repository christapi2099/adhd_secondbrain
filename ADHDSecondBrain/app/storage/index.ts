// Export everything from StorageContext
export {
  AppStorageProvider,
  useStorageContext,
  useStorage,
  useObject,
  useQuery,
} from './StorageContext';

// Export helper functions
export { createObjectId, getCurrentTimestamp } from './utils';

// Export types
export type { 
  // We'll define these types later when we need them
};